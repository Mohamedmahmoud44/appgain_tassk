import 'package:flutter/material.dart';

class CachedNetworkImageReusableComponent extends StatelessWidget {
  const CachedNetworkImageReusableComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
